<div class="contents">
<% if signed_in? %>
<div>
  <table>
    <thead>
      <th>
        <tr>title
        </tr>
        <tr>
          description
        </tr>
        <tr>
          price
        </tr>
      </th>
    </thead>
    <tbody>
    <% @items.each do |item| %>
    <tr>
    <td>
      <%= item.title %>
  </td>
  <td>
      <%= item.description %>
  </td>
  <td>
      <%= item.price %>
  </td>
</tr>
  <% end %>
</table>
</div>

<% else %>

<div>
<% @items.each do |item|  %>
<p><%= item.title %></p>
<% end %>
<% end %>

<%= link_to "Upload items", new_item_path, method: :get %>
</div>
</div>
